## Primeira instalação (comando único)

```bash
sudo apt update && sudo apt upgrade -y
```

```bash
sudo apt install -y git && git clone https://github.com/foxxsouza/installer.git && cd installer && chmod +x install.sh install_primaria install_instancia scripts/*.sh && sudo ./install.sh
```
